# هاي المتغيرات بيضيفها Vercel تلقائيًا لما تربط قاعدة بيانات Upstash Redis
# (Storage tab -> Create Database -> Upstash). ما تحتاج تعبيها يدويًا على Vercel نفسه.
# للتجربة المحلية فقط: شغّل `vercel env pull .env.local` بعد ربط قاعدة البيانات.

KV_REST_API_URL=
KV_REST_API_TOKEN=
